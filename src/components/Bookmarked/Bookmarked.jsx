import React from 'react';
import "./Bookmarked.css";

const Bookmarked = (props) => {
    return (
        <div>
            <h4>{props.title}</h4>
        </div>
    );
};

export default Bookmarked;
